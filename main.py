from imager import read_image, show_image, load_image, resize_image_if_needed
from drawer import draw_action
from gui import GUI
import time

if __name__ == '__main__':
    # time.sleep(3)
    # image = resize_image_if_needed(load_image("test4.jpg"))
    # points = read_image(image)
    # show_image(image, points, 1)
    # draw_action(points_sequence=points)
    GUI()